FROM python:3.13-slim

RUN pip install --no-cache-dir pyftpdlib==2.1.0 pyopenssl==25.3.0 \
    && mkdir -p /srv/ftp/upload

COPY ftp_server.py /usr/local/bin/ftp_server.py

EXPOSE 21 30000-30109
ENTRYPOINT ["python", "/usr/local/bin/ftp_server.py"]
